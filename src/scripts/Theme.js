// theme.js
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#EC6B22',
    },
    secondary: {
      main: '#2196F3',
    },
  },
});

export default theme;
